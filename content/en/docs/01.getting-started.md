---
title: "Getting started"
navigation:
  title: "Getting started"
---

A reference for developers and agents: how to write a CS 1.6 plugin in
TypeScript. The website is built from these pages, so each one reads on its
own.

What is planned and why it was decided that way is in `docs/API-DESIGN.md`.
How to write the code is in the `code-style` skill. This reference covers only
what already exists.

## Your first plugin

```ts
import { Player, print, cmd, plugin, server } from "~/facade";

plugin({ name: "Hello", version: "1.0.0", author: "you", description: "An example" });

cmd("say /hp", sayHp);
server.addEventListener("putinserver", (event) => {
	print(0, `${event.player.name} joined`);
});

function sayHp(id: number) {
	const player = new Player(id);
	print(id, `${player.name}, your HP: ${player.health}`);

	if (player.health < 50) player.health = 100;
}
```

Put the file in `addons/xen/plugins` and its name in `plugins.ini`. The
server compiles it on save and loads it without a map change. From the
repository: `bun run plugins -- --deploy`.

## Hot reload: `bun run dev`

```sh
bun run dev
```

The command builds every plugin and deploys it, then watches `as/`. When you
save a file, it rebuilds the plugins that import that file, copies them to the
server and reloads the running server over rcon:

```
rebuilt nhnse-core (6.8s) -> deployed -> reloaded
  [xen] reloading
```

A change to the facade or to a library rebuilds every plugin that imports it.
A compile error shows as `as/file.ts:line:col - message` above the line it
points at. The server keeps the last good build.

- `XEN_SERVER` in `.env` says where the server is, as it does for `--deploy`.
- The reload goes through rcon to `127.0.0.1:27015` (set `XEN_PORT` to use
  another port). The password is read from `rcon_password` in the server's
  `cstrike/server.cfg`. Without a password the module still reloads a changed
  `.aot` by itself, but you do not see its reply.
- A **new** plugin is named in `plugins.ini`, but it loads only after a server
  restart (`bun run server`). The command tells you when this happens.
- If the server is not running, the command only builds and deploys.
  It never starts or stops hlds.

## Sections

| page | what's inside | status |
| --- | --- | --- |
| [Plugin](/docs/plugin) | `plugin`, commands, events, timers, chat | works on the server |
| [Players and entities](/docs/entities) | `Player`, `Entity`: typed properties | verified on the server |
| [Players and entities](/docs/entities#vectors-vector) | `Vector`, `Entity.findAll` / `create` / `remove` | verified on the server |
| [Players: actions](/docs/players) | `Player.all`, `hasModule`, team, actions (`give`, `respawn`, …) | verified on the server |
| [Flags](/docs/flags) | flags as arrays of names: `player.hideHud = ["Money"]` | verified on the server |
| [Hookchains](/docs/hooks) | the game's events (reapi hookchains): `game.addEventListener("takeDamage", ...)` | verified on the server |
| [Forwards](/docs/forwards) | your own forwards for other plugins: `new Forward<number>("name").emit(1)`, `.subscribe(...)` | verified on the server |
| [Storage](/docs/storage) | storage that survives map changes, like a `Map`: `new Storage("name").get(key)` | verified on the server |
| [Extensions](/docs/extensions) | wrappers over third-party modules: `fetch` over easy_http | verified on the server |
| [Files](/docs/fs) | files like Node: `fs.readFileSync`, `fs.writeFile`, `fs.readdir` | verified on the server |
| [Natives](/docs/natives) | your own natives for Pawn plugins: `export function` | verified on the server |
| [Natives](/docs/natives#calling-natives-directly) | AMXX/reapi natives directly, when the facade lacks something | verified on the server |

## What's different about the language

A plugin is ordinary TypeScript, but AssemblyScript compiles it to
WebAssembly. The differences you will notice:

- `number` is JavaScript's number, whole or not: `7 / 2` is `3.5`, and
  `` `${hp}` `` prints `"100"`. Bitwise operators take it as int32, as in JS.
  A Pawn cell is an integer, but that is the hood's business: natives and the
  facade convert on their own.
- A handler is a named function, not a closure that captures variables:
  AssemblyScript does not support capturing.
- A string union (`"Money" | "Timer"`) works as in TypeScript: the editor
  suggests and checks it, the compiler sees `string`. An open one, where any
  other string goes too, is written as in TypeScript: `"normal" | "dm" |
  (string & {})` - the editor still suggests the literals. There are no
  other unions except `T | null`.
- `a ?? b` and `a ??= b` work as in TypeScript: the right side is taken only
  when the left is `null` (not `""` or `0`), and
  `ini.getValue(section, "KEY") ?? ""` is a `string`.
- A function goes where a longer function type is expected, as in
  TypeScript: `server.addCommand("/gel", gel)` with `function gel(player:
  Player)` gets the player and not the arguments - no `(player, args) =>
  gel(player)` needed.
